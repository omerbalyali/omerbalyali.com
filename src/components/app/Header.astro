---
import ColorSchemePicker from "./ColorSchemePicker.astro";

const isHome = Astro.url.pathname === "/";
---

<header>
	<a href="/" class="brand"><span class:list={[{ "visually-hidden": isHome }]}>Ömer Balyalı</span></a>
	<div class="actions">
		<ColorSchemePicker />
	</div>
</header>

<style>
	header {
		position: relative;
		inset-block-start: 0;
		inset-inline: 0;
		padding: var(--t-app-gutter);
		display: grid;
		grid-template-columns: auto 1fr auto;
		gap: var(--t-space-4);
		align-items: center;
	}

	.brand {
		grid-column: 1;
		justify-self: start;
		transition: var(--t-transition-default);
		transition-property: background-color;
		display: flex;
		gap: var(--t-space-4);
		align-items: center;
		text-decoration: none;

		&::before {
			content: "";
			display: block;
			width: 1.25rlh;
			aspect-ratio: 1 / 1;
			background-color: currentColor;
			mask-image: var(--t-app-brand-logomark);
			mask-size: contain;
			mask-repeat: no-repeat;
			mask-position: center;
		}

		& > span {
			line-height: 1;
			font-size: 1.675em;
			font-size: var(--t-font-size-large);
			padding-block-start: 0.1375rlh;
			font-weight: var(--t-font-weight-bold);
			text-wrap: nowrap;
		}

		&:hover::before {
			color: var(--t-color-brand-primary);
		}
	}

	.actions {
		grid-column: 3;
		justify-self: end;
		display: flex;
		gap: var(--t-space-2);
		align-items: stretch;
		height: var(--t-space-12);
	}
</style>
